language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis Ci 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

# CI 실행완료 시 메일로 알람
notification:
  email:
    recipients:
      - 'deert101@gmail.com'
